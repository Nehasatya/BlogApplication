<%= form_with(model: [@topic, @post , comment] , local:true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :com_description %>
  </div>

  <div class = "field">
    <%= form.hidden_field :author_name, value: current_user.email %>
  </div>


    <div class="actions">
    <%= form.submit%>
  </div>
<% end %>